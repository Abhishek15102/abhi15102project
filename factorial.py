name=input("Enter your name ")
number=int(input("Enter the number for factorial"))
if number=0:
    print(name,"your number is Zero \nwe learn feom child hood if we try to find factorial of zero we factorial is return 0:)")
if n
